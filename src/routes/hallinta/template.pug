div
  h2 {{ $t('hallinta') }}
  table.table
    thead
      tr
        th {{ $t('nimi') }}
        th {{ $t('koulutustyyppi') }}
        th {{ $t('tila') }}
        th {{ $t('toiminto') }}
    tbody
      tr(v-for="ops in adminlist", :key="ops.id")
        td
          router-link(tag="a", :to="{ name: 'pohjanTiedot', params: { id: ops.id } }")
            ep-content(v-model="ops.nimi")
        td {{ $t(ops.koulutustyyppi) }}
        td {{ $t(ops.tila) }}
        td
