# Use travis in https://github.com/Opetushallitus/eperusteet-ylops to deploy
# This is only for running tests

language: node_js

node_js:
  - "10.14.2"

install:
  - npm ci

script:
  - npm run ci:start

cache:
  npm: true
  directories:
    - $HOME/.cache
